using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SFXManager : MonoBehaviour
{
    /*
    [SerializeField] private AudioSource[] audioSources = null;

    private static SFXManager instance;

    private int currentAudioSourceIndex;

    public static SFXManager Instance { get => instance; }

    private void Awake()
    {
        instance = this;
    }

    public void PlaySFX(AudioClip audioClip)
    {
        AudioSource audioSource = audioSources[currentAudioSourceIndex];
        audioSource.PlayOneShot(audioClip);
        this.currentAudioSourceIndex = (currentAudioSourceIndex + 1) % audioSources.Length;
    }
    */
}
